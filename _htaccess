Options +FollowSymLinks

RewriteEngine On

RewriteCond %{REQUEST_FILENAME} -f [OR]
 #accedes a un fichero
RewriteCond %{REQUEST_FILENAME} -d
#acceder o a un directorio
RewriteRule ^.*$ index.php [NC,L]
#regla con el pricipio todoo y el final NC-no hacer cao a mayes y L el final-ultima regla

